# PORTFOLIO REVIEW — {{ today }}

**Internal. Investment Committee briefing.**

## 1. Snapshot

| | |
|---|---|
| Deals under monitoring | {{ deals | length }} |
| Total committed        | CAD {{ "{:,.0f}".format(total_committed) }} |
| Sectors                | {{ sectors | join(", ") }} |
| Watchlist deals        | {{ watchlist | length }} |
| Breach deals           | {{ breached  | length }} |

## 2. Deal-level KPIs

| Deal | Sector | Commitment | DSCR | LLCR | Debt balance | Status |
|---|---|---|---|---|---|---|
{% for d in deals %}
| {{ d.id }} | {{ d.sector }} | {{ d.currency }} {{ "{:,.0f}".format(d.commitment) }} | {{ d.DSCR | ratio if d.DSCR is not none else "—" }} | {{ d.LLCR | ratio if d.LLCR is not none else "—" }} | {{ d.DebtBalance | money(d.currency) if d.DebtBalance is not none else "—" }} | {{ d.status }} |
{% endfor %}

## 3. Watchlist

{% if watchlist %}
{% for d in watchlist %}
- **{{ d.id }}** — {{ d.watch_summary }}
{% endfor %}
{% else %}
_No deals on watchlist this period._
{% endif %}

## 4. Breaches

{% if breached %}
{% for d in breached %}
- **{{ d.id }}** — {{ d.breach_summary }}. Notice drafted; pending sign-off.
{% endfor %}
{% else %}
_No deals in covenant breach this period._
{% endif %}

## 5. Sector concentration

| Sector | Commitment | % of book |
|---|---|---|
{% for sector_name, sector_total in sector_totals %}
| {{ sector_name }} | CAD {{ "{:,.0f}".format(sector_total) }} | {{ "{:.1%}".format(sector_total / total_committed) }} |
{% endfor %}

## 6. Activity in the last 30 days

| Event | Count |
|---|---|
| Model updates  | {{ activity.model_update | default(0) }} |
| Covenant tests | {{ activity.covenant_test | default(0) }} |
| Notices drafted| {{ activity.notice_draft | default(0) }} |
| Backups taken  | {{ activity.model_backup | default(0) }} |
| Restores       | {{ activity.model_restore | default(0) }} |

## 7. Items for committee

_To be completed by the portfolio manager — material developments, sector
trends, requested approvals, upcoming reviews._

---

Distribution: Investment Committee · Credit Risk · Portfolio Management
Generated by `fmcli report portfolio` from live deal data.
